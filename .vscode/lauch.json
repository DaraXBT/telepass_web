{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "MCP via npm",
      "type": "mcp",
      "request": "launch",
      "command": "npm",
      "args": ["run", "mcp-server"],
      "cwd": "${workspaceFolder}"
    }
  ]
}
